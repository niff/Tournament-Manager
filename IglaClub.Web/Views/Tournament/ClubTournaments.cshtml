@model IglaClub.Web.Models.ViewModels.TournamentListViewModel

<div class="row">
    <div class="col-md-12">
        @foreach (var list in @Model.TournamentsList)
        {
            @Html.Partial("_TournamentList", @list)
        }
    </div>
</div>